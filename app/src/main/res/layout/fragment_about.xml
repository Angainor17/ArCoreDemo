<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="com.simferopol.app.ui.about.AboutVm" />

    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/previewImage"
                android:layout_width="match_parent"
                android:layout_height="130dp"
                android:scaleType="centerCrop"

                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:loadImage="@{vm.about.preview}" />

            <ImageView
                android:id="@+id/logo"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:scaleType="centerCrop"
                android:src="@mipmap/simferopol_logo"

                app:layout_constraintBottom_toBottomOf="@+id/previewImage"
                app:layout_constraintEnd_toEndOf="@+id/previewImage"
                app:layout_constraintStart_toStartOf="@+id/previewImage"
                app:layout_constraintTop_toTopOf="@+id/previewImage" />


            <TextView
                android:id="@+id/description"
                style="@style/Description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginTop="32dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="32dp"
                android:text="@{vm.contentText}"

                app:layout_constraintTop_toBottomOf="@id/previewImage" />

            <include
                android:id="@+id/player"
                layout="@layout/audio_player_element"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="106dp"
                app:layout_constraintTop_toTopOf="@+id/previewImage" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.core.widget.NestedScrollView>

</layout>